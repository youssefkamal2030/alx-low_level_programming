#include <stdio.h>

/**
 * main - variables_if_else_while
 *
 * Description: '5-print_numbers.c'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char i;
for (i = 0; i < 10; i++)
{
printf("%d", i);
}
putchar('\n');
return (0);
}
